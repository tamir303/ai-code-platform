[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Unit tests - pure logic, all dependencies mocked
    integration: Integration tests - FastAPI TestClient with in-memory DB
    e2e: End-to-end tests - full user journey flows
